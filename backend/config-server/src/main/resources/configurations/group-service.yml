server:
  port: 8088
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skillsync_groups
    username: skillsync
    password: skillsync
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
