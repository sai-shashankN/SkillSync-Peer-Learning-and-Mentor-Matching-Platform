spring:
  rabbitmq:
    host: rabbitmq
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:skillsync}

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
